---
import { Image } from "astro:assets";

import Layout from "../layouts/Layout.astro";
import NavBar from "../components/NavBar.astro";

import aboutPic from "../img/about_pic.png";

import unreal from "../img/skills/unreal.png";
import figma from "../img/skills/figma.png";
import blender from "../img/skills/blender.png";
import ab_illustrator from "../img/skills/ab_illustrator.png";

const skills = [
  {
    name: "Unreal Engine",
    logo: unreal,
  },
  {
    name: "Figma",
    logo: figma,
  },
  {
    name: "Blender",
    logo: blender,
  },
  {
    name: "Adobe Illustrator",
    logo: ab_illustrator,
  },
] as const;
---

<Layout title="Sobre | Viviane">
  <NavBar />
  <main class="flex h-full items-center justify-center">
    <div class="grid max-w-screen-xl grid-cols-6 px-24">
      <div class="col-span-2">
        <Image
          class="h-full w-full object-cover rounded-xl shadow-2xl"
          src={aboutPic}
          width="385"
          height="550"
          alt="Viviane Diniz's picture"
          loading="lazy"
        />
      </div>

      <div class="col-span-4 ml-24">
        <div class="mb-12">
          <h1 class="font-serif text-[64px]">Viviane Diniz</h1>

          <div class="relative flex items-center">
            <div class="flex-grow h-1 bg-green-primary"></div>
            <div class="p-2 rounded-full bg-green-primary"></div>
          </div>
        </div>

        <div>
          <p class="font-sans text-xl font-semibold	text-justify
            text-pretty">
Sou uma jovem designer e artista 3D brasileira, apaixonada por transformar
            ideias em artes vibrantes e envolventes. Cada projeto do meu portfólio
            reflete minha dedicação em dar vida a conceitos por meio do design
            gráfico e da modelagem tridimensional, proporcionando experiências visuais
            únicas.
 <br />
Deixe-me apresentar sua história de uma forma especial.
          </p>
        </div>

        <div class="mt-24">
          <h2 class="font-serif text-[32px]">Skills</h2>

          <div class="relative flex items-center">
            <div class="flex-grow h-0.5 bg-purple-primary"></div>
          </div>
        </div>
        <div class="flex justify-between mt-11">
          {
            skills.map((skill) => (
              <div class="bg-purple-primary rounded-full flex items-center
                shadow-2xl shadow-purple-primary">
                <div class="bg-purple-secondary rounded-full p-3">
                  <Image
                    class="max-h-11 max-w-11"
                    src={skill.logo}
                    alt={`Logo of ${skill.name}`}/>
                </div>
                <p class="text-wrap pl-1 pr-2 font-sans text-xl">{skill.name}</p>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </main>
</Layout>

<style>
  .rounded-img {
    border-radius: 10px;
  }
</style>
